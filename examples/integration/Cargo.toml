[package]
name = "integration"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[lib]
name = "integration"
path = "src/lib.rs"

[[example]]
name = "integration-win"
path = "examples/win.rs"

[dependencies]
lumen-core = { workspace = true }
lumen-render = { workspace = true }
lumen-widgets = { workspace = true }
lumen-layout = { workspace = true }
kurbo = { workspace = true }

[dev-dependencies]
# The point of this example: the HOST owns winit + wgpu; Lumen is a component.
winit = { workspace = true }
wgpu = { workspace = true }
lumen-shell = { workspace = true }
