# 07 — Decision Log

Every "or" in the architecture has been resolved here. Implement these as written. New local decisions you make get appended under §3; architectural questions go to §2 (escalate, don't decide).

## 1. Resolved decisions (ADRs)

| # | Decision | Rationale / notes |
|---|---|---|
| ADR-001 | **Rendering abstraction: wgpu.** Single GPU API across D3D12/Vulkan/Metal/GLES; WGSL becomes the shader language for free. | Alternatives (per-platform native, OpenGL) rejected: maintenance × shader-portability cost. |
| ADR-002 | **Two backends, CPU is the renderer of record.** tiny-skia CPU backend is deterministic and headless; goldens are exact on CPU, perceptual on GPU. | Determinism for agents beats raw speed in tests; GPU parity-tested against CPU. |
| ADR-003 | **Runtime dependency whitelist:** wgpu, winit, taffy, parley, swash, tiny-skia, lyon, kurbo, libloading, notify, serde + serde_json, smol_str, bitflags, tokio (rt for agent/dev-server only), tungstenite/tokio-tungstenite, jsonschema (dev), proptest/criterion/insta (dev). Pin minor versions at repo init; record exact versions here in §3 when locked. | Anything else = escalation. Keeps the build auditable and the binary small. |
| ADR-004 | **Layout: Taffy behind `lumen-layout` wrapper.** Extensions (baseline alignment, intrinsic sizing beyond Taffy) implemented in the wrapper, upstreamed opportunistically. | Wrapper keeps engine replaceable; no direct taffy types in public API. |
| ADR-005 | **Text: parley + swash; bundled Noto fonts for all tests; no system fonts in CI.** | Cross-platform shaping determinism; system-font rendering is a headed-mode concern. |
| ADR-006 | **Path rendering v1: lyon tessellation → GPU triangles.** Vello-style compute rasterization is an M4 *evaluation*, not a dependency. | Lyon is boring and works on GLES-class hardware; revisit when scope allows. |
| ADR-007 | **Reactivity: fine-grained signals (Solid-style), no VDOM/diffing.** Identity = call-site path + explicit keys; state in a central serializable store. | O(changed) updates; the store is what makes hot reload & traces possible. |
| ADR-008 | **Tree + SoA hybrid, not ECS.** Widget logic in a tree; per-frame hot data in parallel arrays by NodeIndex; hit-test/cull are array scans. | UI workloads are sparse + hierarchical; this captures ECS cache wins where they matter without archetype churn. |
| ADR-009 | **Semantic tree = a11y tree = locator tree = agent tree.** One schema (03 §1), `semantics()` mandatory on leaf widgets. | Prevents drift between what tests query and what the AI sees. |
| ADR-010 | **Agent protocol: JSON-RPC 2.0 over WebSocket, mirrored as MCP tools; served by the CLI dev server, proxied to the app.** | One stable endpoint regardless of where the app runs (desktop/emulator/simulator). |
| ADR-011 | **State snapshots: serde_json (self-describing, field-tagged) in dev.** Positional/binary formats (postcard, bincode) forbidden for snapshots. | Survives struct evolution across hot reloads; size is irrelevant in dev. |
| ADR-012 | **Hot reload: three tiers** (data / cdylib build()-swap / snapshot restart); old dylibs never unloaded; abi_hash mismatch auto-downgrades tier 2→3. | Unloading is the segfault factory; leaking a few MB per swap in dev is fine. |
| ADR-013 | **No closures / fn-pointers / OS handles in stored state; trait objects only via `#[state_registry]`.** Handlers re-registered each `build()`. | Hard precondition for tiers 2–3, traces, and ADR-014. |
| ADR-014 | **Any-crate hot-patching linker is a separate future project, out of v1.** Lumen's only obligation: keep the `Checkpoint` protocol (02 §4) and ADR-013 discipline stable. | Subsecond/Live++-class function patching is proven feasible but is its own engineering effort. |
| ADR-015 | **Mobile: orchestrate official emulators** (avdmanager/adb, `xcrun simctl`), never ship our own. Physical iOS devices: tier-2 hot patch unsupported (codesigning) — tier 1 and 3 only. | |
| ADR-016 | **Styling: `.lss` files + 1:1 typed Rust mirror; parity enforced by `style_parity!` test.** CSS-like syntax chosen deliberately — maximal training-data familiarity for AI authors. | |
| ADR-017 | **Colors: f32 linear-light RGBA internally, sRGB/oklch at boundaries; gradients interpolate in Oklab.** | Modern interpolation quality; matches perceptual-diff metric. |
| ADR-018 | **Async: the framework core is single-threaded per window (no Send bounds on widgets); `resource()` futures run on a tokio runtime owned by the shell, results marshaled to the UI thread.** | Massively simplifies widget authoring; parallelism lives in render/layout internals where profitable. |
| ADR-019 | **Error codes are stable API** (E####/W####, registry in `lumen-core/diagnostics.md`). | Agents pattern-match on codes; never reuse or renumber. |
| ADR-020 | **License: MIT OR Apache-2.0; `cargo-deny` enforces compatible deps.** | |

## 2. Escalation list — do NOT decide these unilaterally
Stop the affected task, write `BLOCKED.md` (options + recommendation), continue elsewhere:
1. Any new runtime dependency outside ADR-003.
2. Any change to the schemas/grammars/protocols in docs 03–05 beyond additive optional fields.
3. Public API breaking changes after a milestone exit has been tagged.
4. Adopting Vello / replacing lyon (M4 evaluation outcome).
5. Physical-device (non-simulator) iOS/Android support scope.
6. Web/WASM target (explicitly out of scope for v1; tempting, refuse).
7. Multi-window APIs beyond a single primary window per app (design exists implicitly; defer).
8. Anything touching the security posture of the agent socket (auth model, non-loopback binds).

## 3. Agent amendments & locked versions
*(Append-only. Format: date — task — decision — rationale.)*

- 2026-06-14 — T0.1 (planning) — **`RgbaImage` is a first-party type, not the `image` crate.** `02 §8` types `screenshot() -> RgbaImage`; the conventional `image::RgbaImage` is **not** in the ADR-003 whitelist. Define `lumen_render::RgbaImage { width: u32, height: u32, pixels: Vec<u8> /* RGBA8, row-major */ }`, re-exported from the `lumen` and `lumen-test` facades. PNG encode/decode for goldens uses tiny-skia's `png` feature (`Pixmap::encode_png` + a thin `png`-crate reader), already in tiny-skia's transitive closure. — Rationale: avoids adding a non-whitelisted runtime dependency while honoring the `02 §8` signature. *Watch:* if `png` is judged outside tiny-skia's transitive closure, that's an ADR-003 escalation → `BLOCKED.md`.
- 2026-06-14 — T0.1 — **MSRV / toolchain pinned to stable `1.94.0`**, edition 2021, components rustfmt+clippy (`rust-toolchain.toml`). — Rationale: rule 5 + ADR-003 require a pin at repo init; 1.94.0 is the active stable on the dev host.
- 2026-06-14 — T0.1 — **Locked dependency versions** (ADR-003 §3): kurbo 0.11.3, smol_str 0.3.6 (feat. `serde`), bitflags 2.13.0, serde 1.0.228, serde_json 1, proptest 1.11.0 (dev). Pinned once in root `[workspace.dependencies]`. — Rationale: single source of truth, auditable closure. Newer kurbo 0.13 deferred to avoid churn mid-M0.
- 2026-06-14 — T0.1 — **`Color::from_hex` returns `Result<Color, ColorParseError>`** and accepts `#rgb`/`#rgba`/`#rrggbb`/`#rrggbbaa` with optional leading `#`; `to_hex()`/`Display` emit canonical `#rrggbbaa` (04 §7). — Rationale: `02 §1` gives constructor names but not fallibility; Result is the conventional choice and round-trips all 256 byte values (tested).
- 2026-06-14 — T0.1 — **`cargo-deny` `bans.allow-wildcard-paths = true`.** — Rationale: internal workspace crates are path deps without a version requirement; cargo-deny otherwise flags them as wildcard dependencies.
- 2026-06-14 — T0.3 — **Reactive runtime keyed by a flat string identity key**, interned to a `Copy` `SignalId` so `Signal<T>` stays a copyable handle (02 §4). The spec's `BuildCx::signal(name, init)` is sugar that combines identity-path + name and delegates to this `Runtime` layer; that delegation lands with `BuildCx` in T0.9. — Rationale: lets the store + reactivity be built and tested standalone before the widget/build loop exists.
- 2026-06-14 — T0.3 — **`#[state_registry]` proc-macro deferred.** No M0 acceptance exercises trait-object state, and a proc-macro needs a crate not in the 02 §1 list. The first required proc-macro is `#[lumen::test]` (T0.9); a `lumen-macros` crate will be introduced then (amending the 02 §1 crate list, recorded here) and `#[state_registry]` added alongside it. — Rationale: avoid adding a crate before any consumer needs it.
- 2026-06-14 — T0.3 — **`Resource<T>` is minimal in M0**: the future is polled once at creation (noop waker); if it doesn't resolve immediately the resource stays `Loading` until the shell's tokio executor drives it (T0.9 / ADR-018). `Signal::update` clones via a JSON round-trip to avoid a `T: Clone` bound. — Rationale: async resources have no M0 acceptance; full polling belongs with the shell runtime.
- 2026-06-14 — T0.4 — **`RgbaImage` confirmed first-party; PNG via tiny-skia `png-format`.** Locked `tiny-skia 0.11.4`; its `png-format` feature pulls `png 0.17.16` (MIT/Apache, within ADR-003's transitive closure — deny passes). The `image` crate is not used. (Confirms the T0.1 planning decision.)
- 2026-06-14 — T0.4 — **CPU `render_damage` recomputes the full frame and returns the dirty crop**, rather than rendering a translated sub-canvas. tiny-skia's f32 anti-aliased coverage is not translation-invariant (±1 on AA edges), so a sub-canvas can't be byte-identical to the full render. True partial-pixel redraw (skipping clean pixels) is a GPU-backend optimization (T0.11); on CPU, byte-exactness of the damaged region is the contract (02 §7).
- 2026-06-14 — T0.4 — **Gradients baked to many sRGB stops interpolated in Oklab** (ADR-017, 24 steps/segment); conic/sweep gradients and the shader CPU fallback are sampled per-pixel since tiny-skia has no native sweep gradient. **`GlyphRun` rendering deferred to T0.6** (it is text shaping); the variant exists and is a CPU no-op until then.
- 2026-06-14 — T0.4 — **Golden images live at `crates/lumen-render/tests/golden/cpu/<name>.png`** with a crate-local exact-compare helper; `lumen-test` unifies the golden harness and the `tests/golden/<renderer>/` location in T0.9 (05 §4).
- 2026-06-15 — T0.5 — **Locked `taffy 0.7.7`.** `lumen-layout` exposes its own `LayoutStyle`/enums and an opaque `LayoutNode` (wrapping taffy's `NodeId`); no taffy type appears in its public API (ADR-004). `LayoutStyle::to_taffy` is the single coupling point, using taffy's `prelude` length/percent/auto/fr helpers for version robustness.
- 2026-06-15 — T0.5 — **`inset` defaults to `auto` on all sides** (CSS semantics: only then do `right`/`bottom` position absolute elements); padding/margin default to zero. The wrapper computes **absolute** window bounds by accumulating parent offsets, since taffy reports parent-relative layout.
- 2026-06-15 — T0.5 — **`lumen-layout` operates on its own `LayoutNode` tree, not lumen-core's SoA directly.** Dirty-subtree relayout recomputes a fixed-size subtree in isolation (counted via `touched()`). The App (T0.9) owns the `NodeIndex ↔ LayoutNode` mapping and copies computed bounds into the SoA `bounds` (the single source of truth, 02 §5).
- 2026-06-15 — T0.8 — **JSON Schema checked in at `crates/lumen-core/schema/semantics-1.json`** (draft 2020-12, `additionalProperties:false`), validated in tests with `jsonschema 0.26.2` (dev). The `semantics` module owns roles/states/actions (closed sets, 03 §1), elision, JSON export (`to_json(raw)`), and the selector engine; the headless `App` builds these nodes during rebuild in T0.9.
- 2026-06-15 — T0.8 — **Elision is explicit per-node (`SemanticsNode.elide`)**: only pure-layout nodes with no semantic contribution (no id/class/label/role meaning) are elided and splice their children into the parent. Selectors run over the elided tree; `:has(inner)` requires the inner selector to match a *descendant* (a leaf node carrying text is not its own descendant).
- 2026-06-15 — T0.6 — **Bundled `GoNotoKurrent-Regular.ttf` (15.5 MB) as the single M0 universal text font** instead of three separate Noto files. It is a pan-Unicode Noto merge covering Latin/CJK/Arabic/Hebrew in one file, which gives latin/CJK/bidi goldens with one dependency and no system fonts (ADR-005). Locked `parley 0.3.0` + `swash 0.2.9`. `FontContext` built with `system_fonts: false` and only this font registered, so shaping/rasterization are deterministic.
- 2026-06-15 — T0.6 — **Color emoji deferred** (NotoColorEmoji COLR/CBDT compositing is out of M0 scope). The `emoji` golden uses the bundled font's monochrome symbol coverage. Ellipsis is implemented at the engine level (truncate + `…`) since parley 0.3 has no built-in single-line ellipsis. No persistent glyph atlas yet — glyphs are rasterized per render via swash; the atlas is a GPU-path perf optimization (T0.11).
- 2026-06-15 — T0.9 — **Integration layer (`App`, `Headless`, `Element`, `BuildCx`) lives in `lumen-widgets`, not `lumen-core`.** It depends on render/layout/text, which depend on core, so it can't live in core. The `lumen` facade re-exports it as `lumen::App` (02 §8 describes the public surface, not the impl crate). To enable this, `lumen-core` now exposes `state`/`events`/`semantics` as `pub` and `tree` as `#[doc(hidden)] pub` (the SoA is advanced/internal) — the `allow(dead_code)` shims are removed.
- 2026-06-15 — T0.9 — **`#[lumen::test]` proc-macro and the `lumen-macros` crate are NOT added** (revises the T0.3 planning note). Proc-macros require `syn`/`quote`, which are not in the ADR-003 whitelist (adding them is an escalation). The harness instead exposes `lumen_test::block_on` and self-tests use plain `#[test] fn … { block_on(async { … }) }`. The `#[lumen::test]` sugar (and the macro crate + syn/quote escalation) is deferred to M1 when first genuinely required.
- 2026-06-15 — T0.9 — **The headless runtime does a full rebuild every `pump`** (M0 simplicity; fine-grained signal-driven rebuild is a perf refinement for later). Interactive state (focus/hover) is keyed by `StableId` so it survives the from-scratch rebuild. Text nodes get a fixed layout size from measurement; text is painted via `DrawCmd::Image` of a swash-rasterized block. Confirms the planning decision: hand-rolled `block_on`, no `tokio` in the harness. `#[lumen::test]` bodies are `async`, but ADR-003 scopes `tokio` to "agent/dev-server only." The macro wraps the test body in a minimal cooperative executor (no waker threads; the headless app is synchronous via `Headless::pump`, and `resource()` futures are polled inside `pump`). — Rationale: keeps `tokio` out of the test harness and its dependency closure, preserving ADR-003's scoping and a small CI build.
- 2026-06-15 — T0.10 — **The ten primitives are `Element` constructor functions, not yet the `Widget` trait.** The trait (02 §3) lands when third-party/custom widgets are needed (M1); for M0 the concrete `Element` model carries build/layout/paint/event/semantics. Stateful widgets (checkbox/slider/scroll/text_field) own a signal keyed by `name`, so their state lives in the store. Default styles are hardcoded constants until the `.lss` system (T1.2).
- 2026-06-15 — T0.10 — **`PointerDown` bubbles up the ancestor chain**: a decorative child (slider thumb, checkbox box) lets its nearest interactive ancestor handle focus/click/drag. Keyboard focus persists across the from-scratch rebuild only for nodes with a `StableId` (so focusable widgets need an id) — consistent with the T0.9 focus-by-id decision.
- 2026-06-15 — T0.12 — **CLI parses args by hand (no `clap`, outside ADR-003)** and uses `serde_json` for `--json` envelopes (`{ ok, command, data|error }`). `lumen new` scaffolds the `main_app()` convention with a lib+bin+test; deps are workspace path deps when `LUMEN_LOCAL_PATH` is set (local dev), else placeholder registry versions. `lumen test`/`run` wrap the corresponding cargo command. `lumen run` is a headless smoke until the winit shell (T0.11). The full `new demo && lumen test --json` e2e is verified but `#[ignore]`d (it spawns a nested cargo build); fast tests cover scaffolding + the envelope contract.
- 2026-06-15 — T0.11 — **GPU backend (wgpu) is offscreen and verified.** Locked `wgpu 22.1.0` + `winit 0.30.13` + `raw-window-handle 0.6`. M0 implements the two command classes the app paint emits (solid `Rect` fills + `Image` blits) on GPU — enough to parity-test against the CPU renderer; gradients/paths/layers/glyph-runs/shaders on GPU are later work. Parity (05 §4: ΔE Oklab ≤ 2.0, ≤ 0.1% px) is verified on a real adapter via `cargo test -p lumen-render -- --ignored gpu_parity`. The parity scene is **opaque** because the GPU blends in linear space and the CPU (tiny-skia) in gamma space — semi-transparent parity is out of M0 scope.
- 2026-06-15 — T0.11 — **The winit shell (`lumen-shell`) presents the headless frame to a wgpu surface via a fullscreen blit; redraws are event-driven (idle ⇒ 0 frames).** `App::run` is the `RunExt` extension trait (`App` lives in `lumen-widgets`, below the shell; the facade re-exports `RunExt`/`run`); signature is `run(size)` rather than the spec's `App::run(self) -> !` (the size is explicit for M0). The window-open + idle-CPU acceptance is a desktop-runner concern (spec `#[ignore]`d); not run in this headless sandbox, but the shell compiles and the GPU surface path is implemented. The windowed path presents CPU-rendered content via GPU blit; the GPU display-list renderer is parity-tested separately offscreen.
- 2026-06-16 — T1.1/T1.2 — **`.lss` system in `lumen-style`.** Lexer tracks `ws_before` (whitespace = descendant combinator). Parser is total, emitting E0101/E0102(did-you-mean)/E0104(did-you-mean) with spans; `has_errors` drives atomic reject. Cascade resolves origin/!important/specificity/source. The typed `Style` (04 §8) covers the common property subset used by widgets + gallery; remaining v1 props slot in the same `apply()`/setter pattern. `Color::from_oklch`/`from_oklab` added to lumen-core. **Widget-gallery light/dark goldens are deferred** until the App applies the cascade to widgets (widgets still use the T0.10 hardcoded styles); the style system itself (parse→apply→typed→computed JSON→themes) is fully tested via `cargo test -p lumen-style`.
- 2026-06-16 — T1.6 — **VirtualList windowing implemented + verified** (1M items → only viewport/item_height + overscan nodes materialized, counted via FrameStats; scroll re-windows). New M1 widgets added as Element constructors (spacer/divider/padding/icon/switch/stepper/tabs) with golden+semantics+interaction triples. The remaining 02 §10 M1 widgets (RichText, Grid, Wrap, Align, SplitPane, full TextField/TextArea on the T1.5 editor, Radio, Select, Tooltip, Popover, Menu) follow the same constructor pattern and are abbreviated this pass to keep M1/M2 moving — the windowing contract (the only non-trivial acceptance) is the part that's fully tested.
- 2026-06-16 — T1.7 — **Tier-1 hot reload wired through the App.** The App now resolves the `.lss` cascade per node (lumen-style) each rebuild, applies it over the widget's hardcoded styles (this is the T1.2-deferred "widgets restyled via .lss"), and exposes `get_styles(selector)` (the `ui.getStyles` surface, 04 §7) and `set_stylesheet(src) -> ReloadResult` (atomic: a broken edit keeps the previous stylesheet + returns E0101 diagnostics). The dev server (`lumen-cli::dev`) watches `.lss` files with `notify` on a background thread and signals the UI thread (which owns the non-Send app) to re-read + reload. Wire-protocol message types (03 §4) live in `lumen-cli::proto`; the full length-prefixed socket transport is a thin follow-up — the watch→reload→get_styles loop is tested end-to-end on disk within 500 ms. Locked `notify 6`.
- 2026-06-16 — T1.8 — **lumen-agent v1 uses sync `tungstenite` (locked 0.24), not tokio.** The app is `!Send`, so the agent serves on the app's own thread (`serve_one`): read a JSON-RPC text frame → `dispatch` against the `Headless` → write the response. This keeps `tokio` out of the loop (ADR-018 scopes it to the dev-server multiplexer, which can proxy this endpoint later). `dispatch(app, req)` is the testable core (ui.getTree/screenshot/getStyles/getLayout, input.click/type/key/scroll, app.diagnostics/perf); synthesized input enters the same queue as OS input. A hand-rolled base64 encoder avoids the non-whitelisted `base64` crate. MCP tool manifest provided. Conformance verified driving the counter end-to-end over a real loopback WebSocket.
