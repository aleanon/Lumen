# name: Mobile

# on:
#   push:
#     branches: [main]
#   pull_request:

# env:
#   CARGO_TERM_COLOR: always

# jobs:
#   # Android: build the shells, then run the device suite on a headless API-34
#   # emulator (M0-exit unmodified, device golden, tier-1 reload, M3-exit).
#   android:
#     name: android (API-34 emulator)
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v5
#       - uses: dtolnay/rust-toolchain@stable
#         with:
#           targets: x86_64-linux-android
#       - uses: Swatinem/rust-cache@v2.9.1
#       - name: install cargo-ndk
#         run: cargo install cargo-ndk --locked
#       - name: enable KVM
#         run: |
#           echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' \
#             | sudo tee /etc/udev/rules.d/99-kvm4all.rules
#           sudo udevadm control --reload-rules && sudo udevadm trigger --name-match=kvm
#       - name: device tests on emulator
#         uses: reactivecircus/android-emulator-runner@v2
#         with:
#           api-level: 34
#           arch: x86_64
#           target: google_apis
#           force-avd-creation: true
#           emulator-options: -no-window -no-audio -no-snapshot -gpu swiftshader_indirect -no-boot-anim
#           script: |
#             export ANDROID_NDK_HOME=$ANDROID_NDK_LATEST_HOME
#             bash scripts/android_orchestrate.sh test
#             bash scripts/android_m3_exit.sh

#   # iOS: build + run the simulator suite on a macOS runner.
#   ios:
#     name: ios (Simulator)
#     runs-on: macos-14
#     steps:
#       - uses: actions/checkout@v5
#       - uses: dtolnay/rust-toolchain@stable
#         with:
#           targets: aarch64-apple-ios-sim
#       - uses: Swatinem/rust-cache@v2.9.1
#       - name: headless + simulator verification
#         run: bash scripts/ios_orchestrate.sh test
